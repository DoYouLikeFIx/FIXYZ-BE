server:
  port: 8082

internal:
  secret: ${INTERNAL_SECRET:local-internal-secret}

spring:
  application:
    name: fep-simulator
  datasource:
    url: ${FEP_SIMULATOR_DB_URL:jdbc:mysql://localhost:3306/fep_simulator_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true}
    username: ${FEP_SIMULATOR_DB_USERNAME:fix}
    password: ${FEP_SIMULATOR_DB_PASSWORD:fix}
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    validate-on-migrate: true
    locations: classpath:db/migration,classpath:db/seed

springdoc:
  api-docs:
    path: /v3/api-docs
    version: OPENAPI_3_0
  swagger-ui:
    path: /swagger-ui/index.html

management:
  server:
    port: ${FEP_SIMULATOR_MANAGEMENT_PORT:18082}
    address: ${FEP_SIMULATOR_MANAGEMENT_ADDRESS:0.0.0.0}
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus,circuitbreakers
  endpoint:
    health:
      probes:
        enabled: true
