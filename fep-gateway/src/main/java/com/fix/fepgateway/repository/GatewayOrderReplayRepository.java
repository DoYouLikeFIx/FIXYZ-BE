package com.fix.fepgateway.repository;

import com.fix.fepgateway.entity.GatewayOrderReplay;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GatewayOrderReplayRepository extends JpaRepository<GatewayOrderReplay, Long> {
}
