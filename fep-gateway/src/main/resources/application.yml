server:
  port: 8083

internal:
  secret: ${INTERNAL_SECRET:local-internal-secret}

spring:
  application:
    name: fep-gateway

springdoc:
  api-docs:
    path: /v3/api-docs
    version: OPENAPI_3_0
  swagger-ui:
    path: /swagger-ui/index.html

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
