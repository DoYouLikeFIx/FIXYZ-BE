name: ci-corebank

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ci-corebank:
    name: ci-corebank
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle

      - name: Verify corebank lane
        run: |
          ./gradlew --no-daemon --stacktrace \
            :core-common:test \
            :corebank-domain:test \
            :corebank-service:test
