name: ci-fep-simulator

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ci-fep-simulator:
    name: ci-fep-simulator
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle

      - name: Verify fep-simulator lane
        run: |
          ./gradlew --no-daemon --stacktrace \
            :core-common:test \
            :fep-simulator:test
